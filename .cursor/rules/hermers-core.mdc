---
description: Hermers 剪報站專案核心行為與安全約定
alwaysApply: true
---

# Hermers 核心

- **Cursor 為唯一 AI 引擎**；Hermes 只做編排與本機 shell，不呼叫外部 LLM API。
- 待辦任務在 `tasks/pending/*/CURSOR_SPEC.md`，Agent 應優先執行。
- 與使用者溝通預設為**繁體中文**。
- **安全**：不得將 `GITHUB_TOKEN`、Telegram token 寫入程式碼或 commit。
- 剪報內容：`staging/` 待審 → 通過後 `dist/` → `publish` / `test_github_push --push`。
