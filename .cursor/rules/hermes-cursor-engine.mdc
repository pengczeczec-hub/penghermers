---
description: Hermes 主動執行器（非手動任務佇列）
alwaysApply: true
---

# Hermes 執行模型

- Hermes 是**萬用 Agent**：`agent/tools.py` 註冊能力，Telegram 自然語言 → 選工具 → 本機執行。
- 日常操作**直接執行**，不要求使用者開 CURSOR_SPEC。
- 擴充新技能：在 `tools.py` 新增 `_register`；複雜邏輯在 `executor.py` 或新模組實作。
- LLM 可選（OPENAI / Ollama）；無 LLM 時用 `agent/rules.py` 關鍵字路由。
- Git：`gh auth login` 優先於失效的 GITHUB_TOKEN。
